# Mutability Lab

This lab is a collection of small, runnable experiments designed to build
deep intuition about Python's mutability, copying, and function semantics.

Run all experiments:

```bash
python -m mutability_lab
